# 验证测试计划 (EDF TVC 测试台阶段)

> 当前阶段：**阶段六 — 物理验证期**  
> 载具：EDF 涵道风机 TVC 倒立摆测试台（非固体火箭）

---

## 已完成的验证 ✅

| 阶段 | 内容 | 结果 |
|---|---|---|
| **SIL 软件验证** | 卡尔曼滤波、PID 算法数值仿真 | ✅ 通过 |
| **动力学极限仿真** | 50ms 延迟 + SG92R 死区下的 PID 收敛边界 | ✅ 圈定安全参数范围 |
| **传感器硬件验证** | MPU6500 + BMP280 I2C 通信，数据读取正确 | ✅ 通过 |
| **双向通信验证** | 地面站 WiFi UDP 接收遥测 + 指令上行到舵机 | ✅ 舵机物理偏转验证 |
| **气动稳定性** | Barrowman 方程计算 CG/CP，裕度 1.36 cal | ✅ 静稳定 |

---

## 当前阶段验证计划（EDF 测试台）

### 测试 1：地面静态舵机标定
- 上电，串口输入 `set_servo:45,135` → 确认两轴舵机响应角度正确
- 输入 `estop` → 确认舵机归中，遥测停止
- 输入 `reset` → 确认恢复
- **通过标准**：舵机实际偏转角与指令角误差 < 5°

### 测试 2：PID 桌面抖动测试
- 开启 `auto_on`，用手轻推测试台使其倾斜
- 观察舵机是否反向响应（抵抗倾斜）
- **注意**：当前 PID (Kp=1.2) 比仿真保守值激进，**必须先用手扶住**！
- **通过标准**：舵机在 2 秒内产生明显的修正响应，不出现正反馈震荡

### 测试 3：回收机构弹射测试
- 组装拉销弹射机构，地面静态测试
- 串口输入 `arm`，等待 15s → 确认自动触发弹射
- 或直接输入 `deploy_chute` → 确认舵机拔销
- **通过标准**：10 次测试成功率 ≥ 9/10

### 测试 4：EDF 悬停测试（牵引绳保护）
- 测试台用绳索限制在 0.5m 高度范围内
- 开启 `auto_on`，缓慢推油门
- 地面站监控 `pitch_flt` / `roll_flt` 波动范围
- **通过标准**：在 ±5° 以内维持 10 秒

---

## 下一阶段（条件满足后）

> 以下为**未来阶段**，当前暂不执行

| 测试 | 前提条件 |
|---|---|
| 室外 EDF 自由飞行 | 牵引绳测试稳定 ≥ 10 次 |
| 固体火箭搭载（水火箭过渡） | EDF TVC 控制律收敛确认 |
| LoRa 长距离遥测替换 WiFi | 需采购 SX1278 模块并开发固件 |
